#include "search_engine.h"

vise::search_engine::search_engine() {
  std::cout << "Initialized a search engine"
            << std::endl;
}

vise::search_engine::~search_engine() {
  std::cout << "Destroying search engine"
            << std::endl;
}
